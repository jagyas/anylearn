
FROM node:alpine

WORKDIR /app

COPY package.json yarn.lock ./

# install dependencies
RUN yarn --frozen-lockfile

COPY . .

# lint & test
RUN yarn lint & yarn test

EXPOSE 3001

# Running the app
ENTRYPOINT ["npm", "run"]
